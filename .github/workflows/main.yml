name: Build PDF and Publish Release

on:
  push:
    branches:
      - main # Trigger on pushes to the `main` branch (or whichever branch you want)

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Git repository
        uses: actions/checkout@v2
      - name: Compile LaTeX document
        uses: dante-ev/latex-action@latest
        with:
          root_file: book.tex
      - name: Generate a tag
        run: |
          TAG_NAME="v$(date +'%Y%m%d%H%M%S')"  # Generate a tag based on the current date and time
          git tag $TAG_NAME
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git push origin $TAG_NAME  # Push the tag to the repository
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          files: book.pdf
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
